# Architecture Decisions

Decision records explain durable choices and the alternatives that were considered. They are append-only history: supersede an accepted decision with a new record rather than rewriting the old outcome.

Use `template.md`, assign the next four-digit number, and set the status to `proposed`, `accepted`, `rejected`, `deprecated`, or `superseded`.
