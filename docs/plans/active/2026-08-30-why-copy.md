---
title: Trim /why atom aside and expand talk/docs links
status: active
created: 2026-08-30
updated: 2026-08-30
owners:
  - human
  - opencode
---

# Goal

Two copy fixes on `/why`: remove the " — roughly 620,447,501,314,952,912 decks for every single atom — " aside from the first section's atom paragraph, and make talk mode and the technical documentation discoverable from `/why`.

# Non-Goals

- Removing the atoms comparison everywhere: the `/how` closing section and the talk-mode 52-card slide keep their versions of the point — the cut is only the parenthetical aside in `/why`'s first section, which repeats what `/how` says better.
- Rewriting the rest of `/why`.

# Context

- The aside interrupts the thought-experiment paragraph with a 21-digit number that the `/how` page's "Then 52 cards" section already lands with full context.
- `/why` currently never mentions `/talk` or the repo's technical docs (`docs/architecture.md`, decision records, benchmarks) — yet "presenting this material?" is exactly the reader `/why` attracts. `/how` links to talk mode; `/why` should too, plus a pointer to the technical documentation for the "wait, how does this work?" reader who wants depth.

# Dependencies

None.

# Proposed Changes

- `WhyPage.tsx`: delete the aside (and the now-unused `compareDeckCountToAtomsOnEarth` import if nothing else on the page uses it).
- Add a closing line or short section linking to `/talk` (presenting this material) and to the technical documentation in the repo (architecture and decision records), mirroring the pattern `/how` already uses for talk mode.

# Test Plan

- Existing suites pass; no spec should assert on the removed sentence — verify and adjust if one does.

# Benchmark Plan

None.

# Security Considerations

None.

# Documentation Changes

None (the change _is_ documentation-adjacent copy).

# Tasks

- [ ] Remove the atom aside from `/why`.
- [ ] Add talk-mode and technical-docs links.
- [ ] Run quality gates; record evidence.

# Decisions Made

- The atoms comparison stays on `/how` and in talk mode; only the `/why` parenthetical is cut.

# Deviations

None yet.

# Verification Evidence

Pending.

# Outcome

Pending.

# Related Commits

Pending.
