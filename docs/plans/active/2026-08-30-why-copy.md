---
title: Trim /why atom aside and expand talk/docs links
status: active
created: 2026-08-30
updated: 2026-09-01
owners:
  - human
  - opencode
---

# Goal

Remove the long atoms aside from `/why`, then make Arrange, the factoradic explainer, talk mode, and the repository's technical documentation discoverable from the page.

# Non-Goals

- Removing the atoms comparison everywhere: the `/how` closing section and the talk-mode 52-card slide keep their versions of the point — the cut is only the parenthetical aside in `/why`'s first section, which repeats what `/how` says better.
- Rewriting the rest of `/why`.

# Context

- The aside interrupts the thought-experiment paragraph with a 21-digit number that the `/how` page's "Then 52 cards" section already lands with full context.
- `/why` currently never mentions `/arrange`, `/talk`, or the repo's technical docs (`docs/architecture.md`, decision records, benchmarks). `/how` links to talk mode; `/why` should connect readers to the exact-deck interaction, the explanation behind its number, the presentation, and deeper technical records.
- The completed Arrange plan deliberately moved explicit cross-linking here so the shipped, verified editor plan could close. Broader motion evaluation lives in `2026-09-01-talk-motion.md` rather than this bounded copy plan.

# Dependencies

None.

# Proposed Changes

- `WhyPage.tsx`: delete the aside (and the now-unused `compareDeckCountToAtomsOnEarth` import if nothing else on the page uses it).
- Add a closing line or short section linking to `/arrange`, `/how`, `/talk`, and the technical documentation in the repo (architecture and decision records).

# Test Plan

- Existing suites pass; no spec should assert on the removed sentence — verify and adjust if one does.

# Benchmark Plan

None.

# Security Considerations

None.

# Documentation Changes

None (the change _is_ documentation-adjacent copy).

# Tasks

- [ ] Remove the atom aside from `/why`.
- [ ] Add Arrange, explainer, talk-mode, and technical-docs links.
- [ ] Run quality gates; record evidence.

# Decisions Made

- The atoms comparison stays on `/how` and in talk mode; only the `/why` parenthetical is cut.

# Deviations

None yet.

# Verification Evidence

Pending.

# Outcome

Pending.

# Related Commits

Pending.
